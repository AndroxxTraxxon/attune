# nfpm configuration for attune-executor
name: attune-executor
arch: "${ARCH}"
platform: linux
version: "${VERSION}"
section: admin
priority: optional
maintainer: "Attune <attune@rdrx.app>"
description: "Attune Executor — Execution orchestration and workflow engine"
vendor: "Attune"
homepage: "https://git.rdrx.app/attune/attune"
license: "Proprietary"

depends:
  - libc6 (>= 2.28)
  - postgresql-16
  - timescaledb-2-postgresql-16
  - rabbitmq-server

contents:
  - src: "${BUNDLE_DIR}/bin/attune-executor"
    dst: /usr/bin/attune-executor
    file_info:
      mode: 0755

  - src: ../systemd/attune-executor.service
    dst: /usr/lib/systemd/system/attune-executor.service
    file_info:
      mode: 0644

  - src: ../config/attune.yaml
    dst: /etc/attune/attune.yaml
    type: config|noreplace
    file_info:
      mode: 0640
      group: attune

  - src: ../config/environment
    dst: /etc/attune/environment
    type: config|noreplace
    file_info:
      mode: 0640
      group: attune

scripts:
  postinstall: ../scripts/postinstall.sh
  preremove: ../scripts/preremove.sh
  postremove: ../scripts/postremove.sh

overrides:
  rpm:
    depends:
      - glibc >= 2.28
      - postgresql16-server
      - timescaledb-2-postgresql-16
      - rabbitmq-server
  archlinux:
    depends:
      - glibc
      - postgresql
      - timescaledb
      - rabbitmq
